SELECT *
FROM dbo.Admin_Questions

WHERE 
    Enabled = 1
    AND MicrossEntity = '{{ GetPOHFA.data?.[0]?.MicrossEntity || "" }}'
    AND Department = '{{ Select_FADepartment.selectedOptionValue || "" }}'