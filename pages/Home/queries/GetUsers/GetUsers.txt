SELECT 
    u.id,
    u.Email,
    u.Name,
    u.IsAdmin,
    u.IsActive,
		u.IsApprover,
		u.Limit,
    STRING_AGG(d.Department, ', ') AS Departments
FROM dbo.Admin_Users u
LEFT JOIN dbo.Admin_UserDepartments d 
    ON u.id = d.UserId
GROUP BY 
    u.id,
    u.Email,
    u.Name,
    u.IsAdmin,
    u.IsActive,
		u.IsApprover,
		u.Limit
ORDER BY u.Email;