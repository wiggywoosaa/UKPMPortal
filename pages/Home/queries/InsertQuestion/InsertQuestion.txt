INSERT INTO dbo.Admin_Questions (
    Department,
    Question,
    YesNo,
    TextLine,
    Enabled,
    MicrossEntity,
    DependsOnQuestionId,
    DependsOnValue,
    RequiresPM
)
VALUES (
    {{ Table_Questions.newRow.Department }},
    {{ Table_Questions.newRow.Question }},
    {{ Number(Table_Questions.newRow.YesNo || 0) }},
    {{ Number(Table_Questions.newRow.TextLine || 0) }},
    {{ Number(Table_Questions.newRow.Enabled || 0) }},
    {{ Table_Questions.newRow.MicrossEntity }},
    {{ Table_Questions.newRow.DependsOnQuestionId || null }},
    {{ Table_Questions.newRow.DependsOnValue || null }},
    {{ Number(Table_Questions.newRow.RequiresPM || 0) }}
)