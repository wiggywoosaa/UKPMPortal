SELECT *
FROM dbo.POH_DepartmentSignOff
WHERE 
    MicrossSalesOrderReference = '{{ appsmith.store.selectedPO }}'
    AND '{{ appsmith.store.selectedPO }}' IS NOT NULL
    AND '{{ appsmith.store.currentVersion }}' IS NOT NULL