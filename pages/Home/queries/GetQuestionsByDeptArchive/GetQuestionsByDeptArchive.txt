SELECT *
FROM dbo.Admin_Questions
WHERE
    Enabled = 1
    AND MicrossEntity = '{{ GetPOHArchive.data?.[0]?.MicrossEntity || "" }}'
    AND Department = '{{ Select_ArchiveDepartment.selectedOptionValue || "" }}'
