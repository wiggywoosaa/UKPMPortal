SELECT u.Email
FROM dbo.Admin_Users u
INNER JOIN dbo.Admin_UserDepartments ud ON ud.UserId = u.id
WHERE ud.Department = '{{ appsmith.store.selectedDepartment }}'
AND u.IsApprover = 1
AND u.Email IS NOT NULL
AND u.Email != ''